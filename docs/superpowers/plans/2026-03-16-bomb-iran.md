# Bomb Iran 实施计划

> **For agentic workers:** REQUIRED: Use superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 基于 Three.js + Cannon-es 构建一个完整的卡通风格 45 度等角轰炸机街机游戏

**Architecture:** Vite 构建的纯 JS 项目。Three.js 负责渲染（卡通着色器），Cannon-es 负责物理（炸弹、碎片），HTML/CSS Overlay 做 UI。游戏分核心引擎层、实体层、系统层、UI 层四大模块。

**Tech Stack:** Vite, Three.js r160+, Cannon-es, Web Audio API, LocalStorage

---

## 文件结构

```
bomb-iran/
├── index.html                          # 入口页面，挂载 canvas 和 UI 容器
├── vite.config.js                      # Vite 配置
├── package.json                        # 依赖和脚本
├── public/
│   └── audio/                          # 音效文件（后续添加）
├── src/
│   ├── main.js                         # 入口，启动 Game
│   ├── core/
│   │   ├── Game.js                     # 游戏主循环，状态机
│   │   ├── SceneManager.js             # Three.js 场景/相机/渲染器/光照
│   │   ├── PhysicsWorld.js             # Cannon-es 物理世界封装
│   │   ├── InputManager.js             # 键盘鼠标输入管理
│   │   └── AudioManager.js             # 音频管理
│   ├── entities/
│   │   ├── Player.js                   # 玩家飞机（Mesh + 物理体）
│   │   ├── Bomb.js                     # 炸弹（抛物线物理体）
│   │   ├── Bullet.js                   # 机枪子弹
│   │   ├── Building.js                 # 可摧毁建筑（碎片物理）
│   │   ├── AntiAir.js                  # 防空炮（AI 射击）
│   │   ├── Missile.js                  # 跟踪导弹
│   │   ├── Pickup.js                   # 道具
│   │   └── Explosion.js                # 爆炸效果（粒子 + 冲击波）
│   ├── levels/
│   │   ├── LevelManager.js             # 关卡加载和切换
│   │   ├── LevelData.js                # 关卡配置数据
│   │   └── Terrain.js                  # 地形生成
│   ├── systems/
│   │   ├── CombatSystem.js             # 战斗逻辑（伤害、碰撞、AI）
│   │   ├── ScoreSystem.js              # 评分系统
│   │   ├── UpgradeSystem.js            # 升级商店
│   │   ├── AchievementSystem.js        # 成就系统
│   │   └── ParticleSystem.js           # 粒子系统（烟雾、火焰、碎片）
│   ├── ui/
│   │   ├── UIManager.js                # UI 状态管理
│   │   ├── screens/
│   │   │   ├── MainMenu.js             # 主菜单
│   │   │   ├── LevelSelect.js          # 关卡选择
│   │   │   ├── Shop.js                 # 升级商店
│   │   │   ├── Achievements.js         # 成就页面
│   │   │   ├── ResultScreen.js         # 关卡结算
│   │   │   ├── PauseMenu.js            # 暂停菜单
│   │   │   └── Cutscene.js             # 过场动画
│   │   └── components/
│   │       └── HUD.js                  # 游戏中 HUD
│   ├── shaders/
│   │   └── ToonShader.js               # 卡通着色器
│   ├── utils/
│   │   ├── constants.js                # 全局常量和配置
│   │   ├── ObjectPool.js               # 对象池
│   │   └── helpers.js                  # 辅助函数
│   └── styles/
│       └── ui.css                      # UI 样式
```

---

## Chunk 1: 项目初始化 & 核心引擎

### Task 1: 初始化 Vite 项目

**Files:**
- Create: `package.json`
- Create: `vite.config.js`
- Create: `index.html`

- [ ] **Step 1: 使用 Vite 创建项目**

```bash
cd /Users/zhangjinhui/Desktop/bomb-iran
npm init -y
npm install three cannon-es
npm install -D vite
```

- [ ] **Step 2: 配置 vite.config.js**

```javascript
// vite.config.js
import { defineConfig } from 'vite';

export default defineConfig({
  root: '.',
  publicDir: 'public',
  server: {
    port: 3000,
    open: true
  },
  build: {
    outDir: 'dist'
  }
});
```

- [ ] **Step 3: 创建入口 index.html**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Bomb Iran - 轰炸伊朗</title>
  <link rel="stylesheet" href="/src/styles/ui.css">
</head>
<body>
  <canvas id="game-canvas"></canvas>
  <div id="ui-container"></div>
  <script type="module" src="/src/main.js"></script>
</body>
</html>
```

- [ ] **Step 4: 创建基础 CSS**

```css
/* src/styles/ui.css */
* { margin: 0; padding: 0; box-sizing: border-box; }
body { overflow: hidden; background: #000; font-family: 'Segoe UI', sans-serif; }
#game-canvas { position: fixed; top: 0; left: 0; width: 100%; height: 100%; }
#ui-container { position: fixed; top: 0; left: 0; width: 100%; height: 100%; pointer-events: none; z-index: 10; }
#ui-container > * { pointer-events: auto; }
```

- [ ] **Step 5: 验证项目启动**

```bash
npx vite --open
```

Expected: 浏览器打开 http://localhost:3000，显示黑色背景页面，无控制台错误

- [ ] **Step 6: 提交**

```bash
git add -A && git commit -m "feat: 初始化 Vite 项目，安装 Three.js 和 Cannon-es"
```

---

### Task 2: 全局常量和工具函数

**Files:**
- Create: `src/utils/constants.js`
- Create: `src/utils/helpers.js`
- Create: `src/utils/ObjectPool.js`

- [ ] **Step 1: 创建全局常量**

```javascript
// src/utils/constants.js

// 游戏状态枚举
export const GAME_STATES = {
  MENU: 'menu',
  LEVEL_SELECT: 'level_select',
  SHOP: 'shop',
  ACHIEVEMENTS: 'achievements',
  CUTSCENE: 'cutscene',
  PLAYING: 'playing',
  PAUSED: 'paused',
  RESULT: 'result'
};

// 武器类型
export const WEAPONS = {
  BOMB: 'bomb',
  GUN: 'gun'
};

// 道具类型
export const PICKUPS = {
  SHIELD: 'shield',
  SPEED: 'speed',
  MEGA_BOMB: 'mega_bomb',
  HEALTH: 'health'
};

// 评级阈值
export const RATINGS = {
  S: 90,
  A: 75,
  B: 50
};

// 玩家默认配置
export const PLAYER_DEFAULTS = {
  speed: 8,
  maxHP: 3,
  bombCooldown: 1000,    // 投弹冷却 ms
  gunCooldown: 150,       // 机枪冷却 ms
  bombBlastRadius: 5,
  gunDamage: 1,
  bombDamage: 10
};

// 物理常量
export const PHYSICS = {
  gravity: -20,
  bombMass: 2,
  debrisMass: 0.5,
  blastForce: 30
};

// 升级配置
export const UPGRADE_CONFIG = {
  bombPower:      { maxLevel: 5, costs: [0, 200, 400, 800, 1500], multipliers: [1, 1.3, 1.6, 2.0, 2.5] },
  gunPower:       { maxLevel: 5, costs: [0, 200, 400, 800, 1500], multipliers: [1, 1.2, 1.5, 1.8, 2.2] },
  speed:          { maxLevel: 5, costs: [0, 200, 400, 800, 1500], multipliers: [1, 1.15, 1.3, 1.45, 1.6] },
  maxHP:          { maxLevel: 3, costs: [0, 500, 1000],           values: [3, 5, 7] },
  shieldDuration: { maxLevel: 3, costs: [0, 300, 600],            values: [5, 8, 12] }
};
```

- [ ] **Step 2: 创建辅助函数**

```javascript
// src/utils/helpers.js

// 角度转弧度
export function degToRad(deg) { return deg * Math.PI / 180; }

// 随机范围
export function randomRange(min, max) { return Math.random() * (max - min) + min; }

// 随机整数
export function randomInt(min, max) { return Math.floor(randomRange(min, max + 1)); }

// 限制范围
export function clamp(value, min, max) { return Math.max(min, Math.min(max, value)); }

// 线性插值
export function lerp(a, b, t) { return a + (b - a) * t; }

// 距离计算（2D, XZ平面）
export function distanceXZ(a, b) {
  const dx = a.x - b.x;
  const dz = a.z - b.z;
  return Math.sqrt(dx * dx + dz * dz);
}
```

- [ ] **Step 3: 创建对象池**

```javascript
// src/utils/ObjectPool.js

// 对象池 - 减少频繁创建和销毁对象的 GC 压力
export class ObjectPool {
  constructor(createFn, resetFn, initialSize = 10) {
    this.createFn = createFn;
    this.resetFn = resetFn;
    this.pool = [];
    this.active = [];
    for (let i = 0; i < initialSize; i++) {
      this.pool.push(this.createFn());
    }
  }

  get() {
    const obj = this.pool.length > 0 ? this.pool.pop() : this.createFn();
    this.active.push(obj);
    return obj;
  }

  release(obj) {
    const idx = this.active.indexOf(obj);
    if (idx !== -1) {
      this.active.splice(idx, 1);
      this.resetFn(obj);
      this.pool.push(obj);
    }
  }

  releaseAll() {
    while (this.active.length > 0) {
      this.release(this.active[0]);
    }
  }
}
```

- [ ] **Step 4: 提交**

```bash
git add src/utils/ && git commit -m "feat: 添加全局常量、工具函数和对象池"
```

---

### Task 3: 核心引擎 - SceneManager

**Files:**
- Create: `src/core/SceneManager.js`
- Create: `src/shaders/ToonShader.js`

- [ ] **Step 1: 创建卡通着色器**

```javascript
// src/shaders/ToonShader.js
import * as THREE from 'three';

// 卡通着色器 - 阶梯式光照 + 描边
export function createToonMaterial(color, options = {}) {
  const { steps = 4, outlineWidth = 0.03 } = options;

  // 使用 MeshToonMaterial 实现基础的卡通着色
  const gradientMap = new THREE.DataTexture(
    new Uint8Array(steps).map((_, i) => Math.round((i / (steps - 1)) * 255)),
    steps, 1, THREE.RedFormat
  );
  gradientMap.minFilter = THREE.NearestFilter;
  gradientMap.magFilter = THREE.NearestFilter;
  gradientMap.needsUpdate = true;

  const material = new THREE.MeshToonMaterial({
    color: new THREE.Color(color),
    gradientMap: gradientMap,
    ...options
  });

  return material;
}

// 创建描边材质（反面扩大法）
export function createOutlineMaterial(color = 0x000000, thickness = 0.03) {
  return new THREE.ShaderMaterial({
    uniforms: {
      outlineColor: { value: new THREE.Color(color) },
      outlineThickness: { value: thickness }
    },
    vertexShader: `
      uniform float outlineThickness;
      void main() {
        vec3 pos = position + normal * outlineThickness;
        gl_Position = projectionMatrix * modelViewMatrix * vec4(pos, 1.0);
      }
    `,
    fragmentShader: `
      uniform vec3 outlineColor;
      void main() {
        gl_FragColor = vec4(outlineColor, 1.0);
      }
    `,
    side: THREE.BackSide
  });
}
```

- [ ] **Step 2: 创建 SceneManager**

```javascript
// src/core/SceneManager.js
import * as THREE from 'three';

// 场景管理器 - Three.js 场景、相机、渲染器、光照
export class SceneManager {
  constructor(canvas) {
    this.canvas = canvas;

    // 渲染器
    this.renderer = new THREE.WebGLRenderer({
      canvas,
      antialias: true,
      alpha: false
    });
    this.renderer.setSize(window.innerWidth, window.innerHeight);
    this.renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
    this.renderer.shadowMap.enabled = true;
    this.renderer.shadowMap.type = THREE.PCFSoftShadowMap;
    this.renderer.setClearColor(0x87CEEB); // 天蓝色背景

    // 场景
    this.scene = new THREE.Scene();
    this.scene.fog = new THREE.Fog(0x87CEEB, 50, 120);

    // 45度等角相机
    this.camera = new THREE.PerspectiveCamera(35, window.innerWidth / window.innerHeight, 0.1, 200);
    this.camera.position.set(0, 30, 30);
    this.camera.lookAt(0, 0, 0);

    // 光照设置
    this.setupLights();

    // 窗口自适应
    window.addEventListener('resize', () => this.onResize());
  }

  setupLights() {
    // 环境光
    const ambient = new THREE.AmbientLight(0xffffff, 0.5);
    this.scene.add(ambient);

    // 主方向光（带阴影）
    this.sunLight = new THREE.DirectionalLight(0xffffff, 1.0);
    this.sunLight.position.set(10, 20, 10);
    this.sunLight.castShadow = true;
    this.sunLight.shadow.mapSize.width = 2048;
    this.sunLight.shadow.mapSize.height = 2048;
    this.sunLight.shadow.camera.near = 0.5;
    this.sunLight.shadow.camera.far = 100;
    this.sunLight.shadow.camera.left = -30;
    this.sunLight.shadow.camera.right = 30;
    this.sunLight.shadow.camera.top = 30;
    this.sunLight.shadow.camera.bottom = -30;
    this.scene.add(this.sunLight);

    // 补光
    const fillLight = new THREE.DirectionalLight(0x88ccff, 0.3);
    fillLight.position.set(-5, 10, -5);
    this.scene.add(fillLight);
  }

  // 相机跟随目标
  followTarget(targetPosition, smoothness = 0.05) {
    const offset = new THREE.Vector3(0, 30, 30);
    const desired = targetPosition.clone().add(offset);
    this.camera.position.lerp(desired, smoothness);
    this.camera.lookAt(targetPosition);
  }

  onResize() {
    this.camera.aspect = window.innerWidth / window.innerHeight;
    this.camera.updateProjectionMatrix();
    this.renderer.setSize(window.innerWidth, window.innerHeight);
  }

  render() {
    this.renderer.render(this.scene, this.camera);
  }
}
```

- [ ] **Step 3: 验证 - 创建临时 main.js 显示场景**

```javascript
// src/main.js (临时测试)
import { SceneManager } from './core/SceneManager.js';
import { createToonMaterial } from './shaders/ToonShader.js';
import * as THREE from 'three';

const canvas = document.getElementById('game-canvas');
const sceneManager = new SceneManager(canvas);

// 测试：创建一个卡通风格的方块
const geo = new THREE.BoxGeometry(2, 2, 2);
const mat = createToonMaterial(0x00ff88);
const cube = new THREE.Mesh(geo, mat);
cube.castShadow = true;
cube.position.y = 1;
sceneManager.scene.add(cube);

// 地面
const ground = new THREE.Mesh(
  new THREE.PlaneGeometry(50, 50),
  createToonMaterial(0xDEB887)
);
ground.rotation.x = -Math.PI / 2;
ground.receiveShadow = true;
sceneManager.scene.add(ground);

// 动画循环
function animate(time) {
  cube.rotation.y = time * 0.001;
  sceneManager.render();
}
sceneManager.renderer.setAnimationLoop(animate);
```

- [ ] **Step 4: 运行验证**

```bash
npx vite
```

Expected: 浏览器显示天蓝色背景，一个旋转的绿色卡通方块在沙色地面上，有阴影效果

- [ ] **Step 5: 提交**

```bash
git add src/ && git commit -m "feat: 实现 SceneManager 和 ToonShader 卡通着色器"
```

---

### Task 4: 核心引擎 - PhysicsWorld

**Files:**
- Create: `src/core/PhysicsWorld.js`

- [ ] **Step 1: 创建物理世界封装**

```javascript
// src/core/PhysicsWorld.js
import * as CANNON from 'cannon-es';
import { PHYSICS } from '../utils/constants.js';

// 物理世界管理器 - 封装 Cannon-es
export class PhysicsWorld {
  constructor() {
    this.world = new CANNON.World();
    this.world.gravity.set(0, PHYSICS.gravity, 0);
    this.world.broadphase = new CANNON.NaiveBroadphase();
    this.world.solver.iterations = 10;

    // 碰撞材质
    this.defaultMaterial = new CANNON.Material('default');
    this.defaultContactMaterial = new CANNON.ContactMaterial(
      this.defaultMaterial, this.defaultMaterial,
      { friction: 0.3, restitution: 0.3 }
    );
    this.world.addContactMaterial(this.defaultContactMaterial);
    this.world.defaultContactMaterial = this.defaultContactMaterial;

    // 物理体与 Three.js Mesh 的关联映射
    this.bodyMeshPairs = [];

    // 碰撞事件回调
    this.collisionCallbacks = new Map();
  }

  // 添加物理体并关联 Mesh
  addBody(body, mesh = null) {
    this.world.addBody(body);
    if (mesh) {
      this.bodyMeshPairs.push({ body, mesh });
    }
    return body;
  }

  // 移除物理体
  removeBody(body) {
    this.world.removeBody(body);
    this.bodyMeshPairs = this.bodyMeshPairs.filter(p => p.body !== body);
    this.collisionCallbacks.delete(body.id);
  }

  // 注册碰撞回调
  onCollision(body, callback) {
    this.collisionCallbacks.set(body.id, callback);
    body.addEventListener('collide', (e) => {
      const cb = this.collisionCallbacks.get(body.id);
      if (cb) cb(e);
    });
  }

  // 在指定位置施加爆炸力
  applyExplosionForce(position, radius, force) {
    this.world.bodies.forEach(body => {
      if (body.mass === 0) return; // 跳过静态物体
      const dist = body.position.distanceTo(position);
      if (dist < radius && dist > 0) {
        const direction = new CANNON.Vec3();
        body.position.vsub(position, direction);
        direction.normalize();
        const strength = (1 - dist / radius) * force;
        direction.scale(strength, direction);
        body.applyImpulse(direction);
      }
    });
  }

  // 每帧更新物理并同步到 Mesh
  update(deltaTime) {
    this.world.step(1 / 60, deltaTime, 3);

    // 同步物理体位置到 Three.js Mesh
    for (const { body, mesh } of this.bodyMeshPairs) {
      mesh.position.copy(body.position);
      mesh.quaternion.copy(body.quaternion);
    }
  }

  // 创建地面物理体
  createGroundBody() {
    const body = new CANNON.Body({
      mass: 0, // 静态
      shape: new CANNON.Plane(),
      material: this.defaultMaterial
    });
    body.quaternion.setFromEulerAngles(-Math.PI / 2, 0, 0);
    this.world.addBody(body);
    return body;
  }
}
```

- [ ] **Step 2: 更新 main.js 验证物理 — 添加一个掉落的方块**

在 main.js 中导入 PhysicsWorld，创建一个物理方块让它掉到地面上。

- [ ] **Step 3: 运行验证**

```bash
npx vite
```

Expected: 卡通方块从空中掉落到地面，有反弹效果（物理碰撞）

- [ ] **Step 4: 提交**

```bash
git add src/core/PhysicsWorld.js && git commit -m "feat: 实现 PhysicsWorld 物理引擎封装"
```

---

### Task 5: 核心引擎 - InputManager

**Files:**
- Create: `src/core/InputManager.js`

- [ ] **Step 1: 创建输入管理器**

```javascript
// src/core/InputManager.js

// 输入管理器 - 键盘和鼠标
export class InputManager {
  constructor() {
    this.keys = {};
    this.keyJustPressed = {};
    this.mousePosition = { x: 0, y: 0 };

    window.addEventListener('keydown', (e) => {
      if (!this.keys[e.code]) {
        this.keyJustPressed[e.code] = true;
      }
      this.keys[e.code] = true;
    });

    window.addEventListener('keyup', (e) => {
      this.keys[e.code] = false;
    });

    window.addEventListener('mousemove', (e) => {
      this.mousePosition.x = (e.clientX / window.innerWidth) * 2 - 1;
      this.mousePosition.y = -(e.clientY / window.innerHeight) * 2 + 1;
    });
  }

  // 按键是否按下
  isKeyDown(code) { return !!this.keys[code]; }

  // 按键是否刚按下（单次触发）
  isKeyJustPressed(code) { return !!this.keyJustPressed[code]; }

  // 获取移动方向向量（WASD / 方向键）
  getMovement() {
    let x = 0, z = 0;
    if (this.isKeyDown('KeyW') || this.isKeyDown('ArrowUp'))    z -= 1;
    if (this.isKeyDown('KeyS') || this.isKeyDown('ArrowDown'))  z += 1;
    if (this.isKeyDown('KeyA') || this.isKeyDown('ArrowLeft'))  x -= 1;
    if (this.isKeyDown('KeyD') || this.isKeyDown('ArrowRight')) x += 1;
    // 归一化
    const len = Math.sqrt(x * x + z * z);
    if (len > 0) { x /= len; z /= len; }
    return { x, z };
  }

  // 每帧结束时调用，清除单次按键状态
  update() {
    this.keyJustPressed = {};
  }
}
```

- [ ] **Step 2: 提交**

```bash
git add src/core/InputManager.js && git commit -m "feat: 实现 InputManager 输入管理器"
```

---

### Task 6: 核心引擎 - AudioManager

**Files:**
- Create: `src/core/AudioManager.js`

- [ ] **Step 1: 创建音频管理器**

```javascript
// src/core/AudioManager.js

// 音频管理器 - Web Audio API 播放音效和背景音乐
export class AudioManager {
  constructor() {
    this.context = null; // 延迟初始化（需要用户交互触发）
    this.sounds = {};
    this.musicVolume = 0.7;
    this.sfxVolume = 0.8;
    this.currentMusic = null;
  }

  // 初始化音频上下文（在用户首次交互时调用）
  init() {
    if (!this.context) {
      this.context = new (window.AudioContext || window.webkitAudioContext)();
    }
  }

  // 注册音效（使用振荡器合成简单音效，无需音频文件）
  registerSynth(name, config) {
    this.sounds[name] = { type: 'synth', config };
  }

  // 播放合成音效
  playSynth(name) {
    if (!this.context) this.init();
    const sound = this.sounds[name];
    if (!sound || sound.type !== 'synth') return;

    const { frequency = 440, duration = 0.2, type = 'square', volume = 1 } = sound.config;

    const oscillator = this.context.createOscillator();
    const gainNode = this.context.createGain();

    oscillator.type = type;
    oscillator.frequency.value = frequency;
    gainNode.gain.value = volume * this.sfxVolume;
    gainNode.gain.exponentialRampToValueAtTime(0.001, this.context.currentTime + duration);

    oscillator.connect(gainNode);
    gainNode.connect(this.context.destination);

    oscillator.start();
    oscillator.stop(this.context.currentTime + duration);
  }

  // 设置音量
  setMusicVolume(v) { this.musicVolume = v; }
  setSfxVolume(v) { this.sfxVolume = v; }
}
```

- [ ] **Step 2: 提交**

```bash
git add src/core/AudioManager.js && git commit -m "feat: 实现 AudioManager 音频管理器"
```

---

### Task 7: Game 主循环和状态机

**Files:**
- Create: `src/core/Game.js`
- Modify: `src/main.js`

- [ ] **Step 1: 创建 Game 主循环**

```javascript
// src/core/Game.js
import { SceneManager } from './SceneManager.js';
import { PhysicsWorld } from './PhysicsWorld.js';
import { InputManager } from './InputManager.js';
import { AudioManager } from './AudioManager.js';
import { GAME_STATES } from '../utils/constants.js';

// 游戏主类 - 管理游戏循环和状态
export class Game {
  constructor(canvas) {
    this.sceneManager = new SceneManager(canvas);
    this.physicsWorld = new PhysicsWorld();
    this.inputManager = new InputManager();
    this.audioManager = new AudioManager();

    this.state = GAME_STATES.MENU;
    this.lastTime = 0;
    this.isRunning = false;

    // 注册基础音效
    this.audioManager.registerSynth('explosion', { frequency: 80, duration: 0.5, type: 'sawtooth', volume: 0.6 });
    this.audioManager.registerSynth('bomb_drop', { frequency: 300, duration: 0.3, type: 'sine', volume: 0.4 });
    this.audioManager.registerSynth('gun_fire', { frequency: 800, duration: 0.05, type: 'square', volume: 0.3 });
    this.audioManager.registerSynth('pickup', { frequency: 600, duration: 0.15, type: 'sine', volume: 0.5 });
    this.audioManager.registerSynth('hit', { frequency: 200, duration: 0.1, type: 'sawtooth', volume: 0.5 });
  }

  // 切换游戏状态
  setState(newState) {
    const oldState = this.state;
    this.state = newState;
    console.log(`游戏状态: ${oldState} -> ${newState}`);
  }

  // 启动游戏循环
  start() {
    this.isRunning = true;
    this.sceneManager.renderer.setAnimationLoop((time) => this.gameLoop(time));
  }

  // 游戏主循环
  gameLoop(time) {
    const deltaTime = (time - this.lastTime) / 1000;
    this.lastTime = time;

    // 避免首帧或过大的 deltaTime
    if (deltaTime > 0.1 || deltaTime <= 0) return;

    // 根据状态更新
    if (this.state === GAME_STATES.PLAYING) {
      this.physicsWorld.update(deltaTime);
      // 后续: 更新实体、战斗系统等
    }

    this.inputManager.update();
    this.sceneManager.render();
  }

  // 停止游戏循环
  stop() {
    this.isRunning = false;
    this.sceneManager.renderer.setAnimationLoop(null);
  }
}
```

- [ ] **Step 2: 更新 main.js 使用 Game 类**

```javascript
// src/main.js
import { Game } from './core/Game.js';

const canvas = document.getElementById('game-canvas');
const game = new Game(canvas);

// 暂时创建一个测试场景
import * as THREE from 'three';
import { createToonMaterial } from './shaders/ToonShader.js';

// 地面
const ground = new THREE.Mesh(
  new THREE.PlaneGeometry(100, 100),
  createToonMaterial(0xDEB887)
);
ground.rotation.x = -Math.PI / 2;
ground.receiveShadow = true;
game.sceneManager.scene.add(ground);
game.physicsWorld.createGroundBody();

// 测试方块（带物理）
import * as CANNON from 'cannon-es';
const boxGeo = new THREE.BoxGeometry(1, 1, 1);
const boxMat = createToonMaterial(0xff6600);
const box = new THREE.Mesh(boxGeo, boxMat);
box.castShadow = true;
game.sceneManager.scene.add(box);

const boxBody = new CANNON.Body({
  mass: 1,
  shape: new CANNON.Box(new CANNON.Vec3(0.5, 0.5, 0.5)),
  position: new CANNON.Vec3(0, 10, 0)
});
game.physicsWorld.addBody(boxBody, box);

// 开始游戏
game.setState('playing');
game.start();

// 全局暴露便于调试
window.game = game;
```

- [ ] **Step 3: 运行验证**

```bash
npx vite
```

Expected: 橙色方块从空中落到地面，有卡通着色效果和阴影。控制台显示 "游戏状态: menu -> playing"

- [ ] **Step 4: 提交**

```bash
git add src/ && git commit -m "feat: 实现 Game 主循环和状态机"
```

---

## Chunk 2: 玩家飞机 & 基础战斗

### Task 8: 玩家飞机实体

**Files:**
- Create: `src/entities/Player.js`
- Modify: `src/main.js`

- [ ] **Step 1: 创建 Player 实体**

创建玩家飞机类，包含 Three.js 网格（用基础几何体组合出飞机形状）和键盘移动控制。

关键功能：
- 用 `BoxGeometry` + `ConeGeometry` 组合出简化的飞机模型
- 卡通材质着色
- WASD 移动（45度等角映射）
- 相机跟随

- [ ] **Step 2: 更新 main.js 放入玩家飞机**
- [ ] **Step 3: 运行验证** — 玩家飞机可以用 WASD 在地面上方飞行，相机平滑跟随
- [ ] **Step 4: 提交**

```bash
git commit -m "feat: 实现玩家飞机实体和移动控制"
```

---

### Task 9: 炸弹实体 & 物理抛物线

**Files:**
- Create: `src/entities/Bomb.js`
- Create: `src/entities/Explosion.js`
- Create: `src/systems/ParticleSystem.js`

- [ ] **Step 1: 创建 Bomb 实体** — 空格键按下时创建炸弹，带 Cannon-es 物理体，继承飞机速度，受重力抛物线下落
- [ ] **Step 2: 创建 Explosion 爆炸效果** — 炸弹碰撞地面时触发爆炸粒子效果（球体扩展 + 碎片精灵）
- [ ] **Step 3: 创建 ParticleSystem** — 管理爆炸粒子的生命周期、位置更新和自动回收
- [ ] **Step 4: 运行验证** — 按空格投弹，炸弹沿抛物线落下，碰地后产生卡通爆炸效果
- [ ] **Step 5: 提交**

```bash
git commit -m "feat: 实现炸弹投放和卡通爆炸效果"
```

---

### Task 10: 机枪子弹

**Files:**
- Create: `src/entities/Bullet.js`
- Modify: `src/entities/Player.js`

- [ ] **Step 1: 创建 Bullet 实体** — 小球几何体，直线射击，带碰撞检测
- [ ] **Step 2: 更新 Player 添加武器切换** — Q 键切换武器，空格键根据当前武器投弹或射击
- [ ] **Step 3: 运行验证** — Q 切换武器，机枪射出子弹
- [ ] **Step 4: 提交**

```bash
git commit -m "feat: 实现机枪子弹和武器切换"
```

---

### Task 11: 可摧毁建筑

**Files:**
- Create: `src/entities/Building.js`

- [ ] **Step 1: 创建 Building 实体** — 多种类型建筑（不同大小和颜色的卡通方块/组合体）
- [ ] **Step 2: 实现摧毁效果** — 被炸弹命中后生成多个碎片物理体，受冲击力飞散，使用 Cannon-es 物理
- [ ] **Step 3: 运行验证** — 在地面放几栋建筑，投弹可以炸飞它们，碎片四处飞散
- [ ] **Step 4: 提交**

```bash
git commit -m "feat: 实现可摧毁建筑和碎片物理效果"
```

---

### Task 12: 防空炮和跟踪导弹

**Files:**
- Create: `src/entities/AntiAir.js`
- Create: `src/entities/Missile.js`

- [ ] **Step 1: 创建 AntiAir 防空炮** — 地面固定目标，自动朝玩家方向射击，射击有冷却时间
- [ ] **Step 2: 创建 Missile 跟踪导弹** — 锁定玩家后发射，有简单追踪 AI，碰到玩家或飞行超时后爆炸
- [ ] **Step 3: 实现伤害系统** — 玩家被命中减生命值
- [ ] **Step 4: 运行验证** — 地面有防空炮射击玩家、导弹追踪玩家、被命中扣血
- [ ] **Step 5: 提交**

```bash
git commit -m "feat: 实现防空炮、跟踪导弹和伤害系统"
```

---

### Task 13: 道具系统

**Files:**
- Create: `src/entities/Pickup.js`

- [ ] **Step 1: 创建 Pickup 道具** — 空中漂浮旋转的发光几何体（护盾/加速/大范围炸弹/生命恢复），飞机飞过拾取
- [ ] **Step 2: 实现道具效果** — 拾取后应用对应 buff
- [ ] **Step 3: 运行验证** — 地图上有漂浮道具，飞过拾取，效果生效
- [ ] **Step 4: 提交**

```bash
git commit -m "feat: 实现道具系统（护盾/加速/大范围炸弹/生命恢复）"
```

---

### Task 14: 战斗系统整合

**Files:**
- Create: `src/systems/CombatSystem.js`

- [ ] **Step 1: 创建 CombatSystem** — 整合所有战斗逻辑：碰撞检测、伤害计算、实体生命周期管理
- [ ] **Step 2: 整合到 Game 主循环** — 每帧调用 CombatSystem.update()
- [ ] **Step 3: 运行完整战斗验证** — 飞行、投弹、机枪、敌方射击回击、道具、建筑摧毁全部正常
- [ ] **Step 4: 提交**

```bash
git commit -m "feat: 整合 CombatSystem 战斗系统"
```

---

## Chunk 3: 关卡系统 & 地形

### Task 15: 地形生成

**Files:**
- Create: `src/levels/Terrain.js`

- [ ] **Step 1: 创建 Terrain** — 根据关卡类型生成不同风格的地面（沙漠/城市/海岸/山地），使用不同颜色和纹理
- [ ] **Step 2: 运行验证** — 可以切换不同地形风格
- [ ] **Step 3: 提交**

```bash
git commit -m "feat: 实现多风格地形生成"
```

---

### Task 16: 关卡数据和管理

**Files:**
- Create: `src/levels/LevelData.js`
- Create: `src/levels/LevelManager.js`

- [ ] **Step 1: 创建 LevelData** — 定义 12+1 关的配置数据（目标类型/数量/位置、敌方配置、道具分布等）
- [ ] **Step 2: 创建 LevelManager** — 根据关卡 ID 加载配置，生成地形、放置建筑/敌人/道具，管理任务目标状态
- [ ] **Step 3: 实现关卡完成条件** — 所有目标摧毁时触发关卡完成
- [ ] **Step 4: 运行验证** — 可以加载不同关卡，场景切换正常
- [ ] **Step 5: 提交**

```bash
git commit -m "feat: 实现关卡数据配置和关卡管理器"
```

---

## Chunk 4: UI 系统

### Task 17: UIManager 和基础 CSS

**Files:**
- Create: `src/ui/UIManager.js`
- Modify: `src/styles/ui.css`

- [ ] **Step 1: 创建 UIManager** — 管理 UI 屏幕切换（show/hide），连接游戏状态机
- [ ] **Step 2: 编写完整 UI CSS** — 主菜单、HUD、弹窗、按钮等全部样式，卡通游戏风格
- [ ] **Step 3: 提交**

```bash
git commit -m "feat: 实现 UIManager 和游戏 UI 样式"
```

---

### Task 18: 主菜单 & 关卡选择

**Files:**
- Create: `src/ui/screens/MainMenu.js`
- Create: `src/ui/screens/LevelSelect.js`

- [ ] **Step 1: 创建 MainMenu** — 游戏 Logo、开始游戏、商店、成就、设置按钮
- [ ] **Step 2: 创建 LevelSelect** — 4 章节展开，各关卡锁定/解锁/评级状态显示
- [ ] **Step 3: 运行验证** — 主菜单显示正常，点击按钮可以切换到关卡选择
- [ ] **Step 4: 提交**

```bash
git commit -m "feat: 实现主菜单和关卡选择界面"
```

---

### Task 19: HUD & 暂停菜单

**Files:**
- Create: `src/ui/components/HUD.js`
- Create: `src/ui/screens/PauseMenu.js`

- [ ] **Step 1: 创建 HUD** — 生命值、任务目标、分数、武器、道具状态实时显示
- [ ] **Step 2: 创建 PauseMenu** — ESC 暂停游戏，显示继续/重新开始/返回主菜单
- [ ] **Step 3: 运行验证** — 游戏中 HUD 正常更新，ESC 暂停弹出菜单
- [ ] **Step 4: 提交**

```bash
git commit -m "feat: 实现游戏 HUD 和暂停菜单"
```

---

### Task 20: 关卡结算 & 过场动画

**Files:**
- Create: `src/ui/screens/ResultScreen.js`
- Create: `src/ui/screens/Cutscene.js`

- [ ] **Step 1: 创建 ResultScreen** — 评分详情（摧毁率/用时/存活率）、S/A/B/C 等级、金币奖励、下一关/重试按钮
- [ ] **Step 2: 创建 Cutscene** — 关卡开始前的任务简报（任务描述 + 目标列表 + 倒计时）
- [ ] **Step 3: 运行验证** — 完整流程：过场动画→游戏→结算
- [ ] **Step 4: 提交**

```bash
git commit -m "feat: 实现关卡结算和过场动画"
```

---

## Chunk 5: 进度系统

### Task 21: 评分系统

**Files:**
- Create: `src/systems/ScoreSystem.js`

- [ ] **Step 1: 创建 ScoreSystem** — 实时分数计算、关卡结算时计算综合评级
- [ ] **Step 2: 实现 LocalStorage 数据持久化** — 保存/读取玩家进度、金币、关卡评级
- [ ] **Step 3: 运行验证** — 通关后评分正确，刷新页面数据仍在
- [ ] **Step 4: 提交**

```bash
git commit -m "feat: 实现评分系统和数据持久化"
```

---

### Task 22: 升级商店

**Files:**
- Create: `src/systems/UpgradeSystem.js`
- Create: `src/ui/screens/Shop.js`

- [ ] **Step 1: 创建 UpgradeSystem** — 读取/更新升级等级，应用到玩家属性
- [ ] **Step 2: 创建 Shop UI** — 展示 5 个升级项、当前等级/费用、购买按钮
- [ ] **Step 3: 运行验证** — 购买升级后金币扣除、属性生效、数据持久化
- [ ] **Step 4: 提交**

```bash
git commit -m "feat: 实现升级商店系统和 UI"
```

---

### Task 23: 成就系统

**Files:**
- Create: `src/systems/AchievementSystem.js`
- Create: `src/ui/screens/Achievements.js`

- [ ] **Step 1: 创建 AchievementSystem** — 定义 20+ 个成就，监听游戏事件触发解锁
- [ ] **Step 2: 创建 Achievements UI** — 成就列表、解锁状态、奖励信息
- [ ] **Step 3: 实现成就弹窗通知** — 解锁成就时在游戏中弹出提示
- [ ] **Step 4: 运行验证** — 满足条件后成就解锁，显示通知，数据持久化
- [ ] **Step 5: 提交**

```bash
git commit -m "feat: 实现成就系统"
```

---

## Chunk 6: 最终整合 & 打磨

### Task 24: 完整游戏流程整合

- [ ] **Step 1: 连通所有系统** — 主菜单→关卡选择→过场→游戏→结算→商店→成就，全流程可跑通
- [ ] **Step 2: 补齐所有关卡数据** — 12+1 关的完整配置
- [ ] **Step 3: 运行完整流程测试**
- [ ] **Step 4: 提交**

```bash
git commit -m "feat: 完成全流程整合"
```

---

### Task 25: BOSS 战设计

- [ ] **Step 1: 设计最终 BOSS** — 特殊大型敌人，多阶段攻击模式
- [ ] **Step 2: 实现 BOSS 战逻辑** — 血量条、攻击模式切换、弱点机制
- [ ] **Step 3: 运行验证** — BOSS 战可以正常进行和击败
- [ ] **Step 4: 提交**

```bash
git commit -m "feat: 实现最终 BOSS 战"
```

---

### Task 26: 视觉和音效打磨

- [ ] **Step 1: 优化卡通着色效果** — 调整颜色、光照、描边参数
- [ ] **Step 2: 完善爆炸和粒子效果** — 更丰富的粒子、屏幕震动
- [ ] **Step 3: 补充所有音效** — 为所有交互添加合成音效
- [ ] **Step 4: 添加 UI 动画** — 按钮悬停、画面过渡、分数飘字
- [ ] **Step 5: 提交**

```bash
git commit -m "feat: 视觉和音效打磨"
```

---

### Task 27: 性能优化和最终测试

- [ ] **Step 1: 实现对象池复用** — 子弹、碎片、粒子全部使用 ObjectPool
- [ ] **Step 2: 使用 InstancedMesh** — 相同几何体的建筑使用实例化渲染
- [ ] **Step 3: 物理体清理** — 及时移除失效的物理体
- [ ] **Step 4: 全流程终测** — 所有关卡通关测试、成就解锁测试、升级商店测试
- [ ] **Step 5: 提交**

```bash
git commit -m "perf: 性能优化和最终测试"
```

---

## 验证计划

### 自动测试
由于是纯前端游戏项目，主要依赖以下验证方式：

```bash
# 启动开发服务器
npx vite

# 构建检查（无报错即通过）
npx vite build
```

### 浏览器测试

每个 Task 完成后在浏览器中验证：

1. **Chunk 1 验证**：打开 http://localhost:3000，确认卡通着色方块显示、物理掉落、无控制台错误
2. **Chunk 2 验证**：WASD 飞行、空格投弹、Q 切换武器、炸弹物理抛物线、爆炸效果、建筑碎片飞散、防空炮射击、导弹追踪、道具拾取
3. **Chunk 3 验证**：不同关卡加载正确、地形风格切换、目标布局合理
4. **Chunk 4 验证**：UI 界面切换流畅、HUD 实时更新、暂停/恢复正常、过场和结算展示正确
5. **Chunk 5 验证**：评分计算正确、升级购买生效、成就解锁和通知
6. **Chunk 6 验证**：完整流程通关、BOSS 战、性能稳定 60fps
